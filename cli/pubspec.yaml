name: cli
publish_to: none
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  http: ^1.3.0
  logging: ^1.2.0
  command_runner:
    path: ../command_runner
  wikipedia:
    path: ../wikipedia

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
